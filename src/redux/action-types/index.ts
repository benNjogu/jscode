export enum CellActionTypes {
  MOVE_CELL = 'MOVE_CELL',
  DELETE_CELL = 'DELETE_CELL',
  INSERT_CELL_AFTER = 'INSERT_CELL_AFTER',
  UPDATE_CELL = 'UPDATE_CELL',
}

export enum BundelActionTypes {
  BUNDLE_CREATED = 'BUNDLE_CREATED',
}
